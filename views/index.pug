extends layout
 
block content
  if (!user)
    a(href="/login") Login
    br
    a(href="/register") Register
  if (user)
    p You are currently logged in as #{user.username}
    a(href="/hillstations") Take me to hillstations
    br
    a(href="/logout") Logout